串行:	一个任务一个任务执行,逐个完成任务

并发:	几个任务不同时执行,但是执行时间有重叠.也就是在其他任务等待的时间执行任务

并行:	几个任务同时执行


并发和并行关系
	并发可以提高对事务的处理效率,在一段时间内处理或完成更多的事情
	并行是理想情况下的并发

	从硬件的角度:	如果单核cpu,一个处理器只能执行一个线程的情况下,处理器可以使用时间片轮转技术
			让cpu快速在各个线程间进行切换,对于用户来说,感觉是三个线程在同时执行,如果是
			多核心cpu,可以为不同的线程分配不同的cpu内核
