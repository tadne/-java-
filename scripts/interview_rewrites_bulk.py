#!/usr/bin/env python3
# -*- coding: utf-8 -*-
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent

BLOCKS = {
"面试题/Java基础/Java中对象的多种创建方式.md": """# Java 中对象的多种创建方式

| 方式 | 说明 |
|------|------|
| new | 最常用，调用构造器 |
| 反射 | `Class.newInstance()`（已过时）/ `getDeclaredConstructor().newInstance()` |
| 克隆 | `clone()`，需实现 `Cloneable` |
| 反序列化 | `ObjectInputStream.readObject()` |
| 工厂/Builder | 封装创建逻辑，非语言内置 |

## 面试一句话

> 常见五种：new、反射、克隆、反序列化，以及工厂/Builder 等封装创建。
""",
"面试题/Java基础/java序列化中有些字段不想进行序列化怎么办.md": """# 序列化中部分字段不想序列化怎么办

## 方式

| 方法 | 说明 |
|------|------|
| `transient` | 修饰字段，默认不参与序列化 |
| `writeObject` / `readObject` | 自定义序列化逻辑 |
| `Externalizable` | 完全自定义读写 |

## 注意

- 反序列化后 `transient` 字段为默认值（null/0）
- 修改 `serialVersionUID` 不当会导致 `InvalidClassException`

## 面试一句话

> 用 `transient` 排除字段，或自定义 `writeObject`/`readObject` 精细控制。
""",
"面试题/Java基础/Java中IO与NIO的区别.md": """# Java 中 IO 与 NIO 的区别

| 维度 | BIO（传统 IO） | NIO |
|------|----------------|-----|
| 模型 | 面向流，阻塞 | 面向缓冲区 + Channel，可非阻塞 |
| API | InputStream/OutputStream | Channel、Buffer、Selector |
| 线程 | 常一线程一连 | 少线程多路复用 |
| 适用 | 简单、连接少 | 高并发网络服务 |

## 面试一句话

> BIO 阻塞流式；NIO 缓冲区+Channel+Selector 支持多路复用与非阻塞。
""",
"面试题/Java基础/Object类常用方法.md": """# Object 类常用方法

| 方法 | 作用 |
|------|------|
| `equals` / `hashCode` | 逻辑相等与哈希（重写约定） |
| `toString` | 字符串表示 |
| `getClass` | 运行时类对象 |
| `clone` | 浅拷贝，需 `Cloneable` |
| `wait` / `notify` / `notifyAll` | 线程协作（须在同步块内） |
| `finalize` | 已废弃，不推荐依赖 |

## 面试一句话

> 必会 equals/hashCode/toString；wait/notify 用于线程通信；clone 浅拷贝。
""",
"面试题/Java基础/OOM和SOF举例.md": """# OOM 和 SOF 举例

| 错误 | 全称 | 常见原因 |
|------|------|----------|
| OOM | OutOfMemoryError | 堆满、元空间满、直接内存满、无法创建线程 |
| SOF | StackOverflowError | 无限递归、栈帧过大 |

## OOM 示例

- `Java heap space`：对象过多、内存泄漏
- `Metaspace`：类加载过多
- `Unable to create new native thread`：线程数超限

## 面试一句话

> OOM 是堆/元空间/直接内存等耗尽；SOF 是虚拟机栈深度溢出，多见于递归过深。
""",
"面试题/Java基础/标识符的命名规则.md": """# 标识符的命名规则

- 由字母、数字、`_`、`$` 组成，**不能以数字开头**
- 不能是关键字或保留字（`true`/`false`/`null` 非关键字但勿作标识符）
- 区分大小写
- 无长度上限（实践保持可读）

## 面试一句话

> 字母/数字/下划线/$ 组成，不以数字开头，非关键字，区分大小写。
""",
"面试题/Java基础/泛型常用特点.md": """# 泛型常用特点

## 作用

编译期类型检查，消除强制转换，提高可读性与安全。

## 要点

| 点 | 说明 |
|----|------|
| 类型擦除 | 编译后泛型信息擦除，运行时无 `List<String>` 类型 |
| 通配符 | `? extends T` 只读；`? super T` 可写 T |
| 不能 | `new T[]`、静态字段类型参数、基本类型作类型参数 |

## 面试一句话

> 泛型提供编译期类型安全；擦除后运行时无泛型实参，通配符 extends/super 控制读写。
""",
"面试题/Java基础/反射作用原理.md": """# 反射作用原理

## 作用

运行时动态获取类信息、创建对象、调用方法、访问字段（可突破 `private` 需 `setAccessible`）。

## 原理

- 类加载后元数据在**方法区**（JDK8+ Metaspace）
- `Class` 对象代表运行时类型，JVM 根据元数据完成动态调用

## 应用

框架（Spring IOC）、动态代理、注解处理

## 缺点

性能低于直接调用；破坏封装

## 面试一句话

> 反射在运行时通过 Class 元数据操作类成员；灵活但有性能与安全问题。
""",
"面试题/Java基础/instanceof关键字作用.md": """# instanceof 关键字作用

## 作用

判断对象是否为某类（或接口）的实例；JDK 16+ 支持模式匹配：

```java
if (obj instanceof String s) { /* 直接用 s */ }
```

## 注意

- `null` 对任何类型 `instanceof` 为 false
- 数组与 `Object`、`Cloneable` 等关系需记清

## 面试一句话

> instanceof 判断运行时类型，模式匹配可顺带强转赋值。
""",
"面试题/Java基础/IO流详情.md": """# IO 流详情

## 分类

| 维度 | 类型 |
|------|------|
| 流向 | 输入流、输出流 |
| 数据 | 字节流（InputStream/OutputStream）、字符流（Reader/Writer） |
| 功能 | 节点流、处理流（缓冲、转换、打印等） |

## 常用

- `Buffered*`：减少系统调用
- `InputStreamReader`：字节→字符，指定 Charset
- `try-with-resources` 自动关闭

## 面试一句话

> IO 分字节/字符、节点/包装流；字符流需编码，缓冲流提升性能，用 try-with-resources 关闭。
""",
"面试题/操作系统/并行和并发的区别.md": """# 并行和并发的区别

| 概念 | 含义 |
|------|------|
| 并发 | 逻辑上同时处理多任务（时间片轮转），单核也可 |
| 并行 | 物理上同一时刻多任务执行，需多核 |

## 面试一句话

> 并发是逻辑交替执行；并行是物理同时执行，依赖多核。
""",
"面试题/操作系统/协程与线程的区别.md": """# 协程与线程的区别

| 维度 | 线程 | 协程 |
|------|------|------|
| 调度 | 内核/OS | 用户态程序 |
| 切换成本 | 较高 | 低 |
| 栈 | 内核栈，MB 级 | 用户栈，KB 级 |
| 阻塞 | 阻塞可能占线程 | 协作式让出，不阻塞线程池 |

## 面试一句话

> 协程是用户态轻量调度单位，切换便宜；线程由 OS 调度，阻塞成本高。
""",
"面试题/操作系统/临界区概念.md": """# 临界区概念

## 定义

访问**共享资源**的那段代码称为临界区；同一时刻只允许一个执行体进入。

## 同步要求

- 互斥进入
- 有限等待
- 空闲让进（可选优化）

## 实现

互斥锁、信号量、自旋锁、CAS 等

## 面试一句话

> 临界区是访问共享资源的代码段，必须用锁或信号量保证互斥。
""",
"面试题/操作系统/进程间的通信方式.md": """# 进程间的通信方式

| 方式 | 特点 |
|------|------|
| 管道 | 半双工，亲缘进程 |
| 命名管道 FIFO | 无亲缘也可 |
| 消息队列 | 结构化消息 |
| 共享内存 | 最快，需配合同步 |
| 信号量 | 同步/互斥 |
| 信号 | 异步通知 |
| Socket | 跨主机 |

## 面试一句话

> IPC 有管道、消息队列、共享内存、信号量、信号、Socket 等，共享内存最快需同步。
""",
"面试题/操作系统/进程间的同步方式.md": """# 进程间的同步方式

- **信号量**（P/V）
- **互斥锁 / 读写锁**
- **管程**（Monitor）
- **条件变量**
- **屏障、事件**

目的：协调对共享资源的访问顺序，避免竞态。

## 面试一句话

> 进程同步靠信号量、锁、管程等，保证共享资源互斥与有序访问。
""",
"面试题/操作系统/线程同步的方式.md": """# 线程同步的方式

| 方式 | 说明 |
|------|------|
| mutex / 临界区 | 互斥 |
| 信号量 | 计数资源 |
| 条件变量 | 等待条件成立 |
| 自旋锁 | 短时忙等 |
| 读写锁 | 读共享写独占 |

Java 对应：`synchronized`、`Lock`、`Semaphore`、`CountDownLatch` 等。

## 面试一句话

> 线程同步用互斥锁、信号量、条件变量等；Java 有 synchronized 与 JUC 工具。
""",
"面试题/操作系统/进程调度策略.md": """# 进程调度策略

| 算法 | 思想 |
|------|------|
| FCFS | 先来先服务，简单可能护航 |
| SJF | 短作业优先，平均等待低，可能饥饿 |
| 时间片轮转 | 公平，响应好 |
| 优先级 | 高优先级先运行 |
| 多级反馈队列 | 综合，兼顾 IO/CPU 型 |

## 面试一句话

> 常见 FCFS、SJF、时间片轮转、优先级与多级反馈队列，权衡吞吐与响应。
""",
"面试题/操作系统/进程和线程的切换流程.md": """# 进程和线程的切换流程

## 触发

时间片用完、阻塞 IO、主动让出、高优先级抢占等。

## 步骤（简化）

1. 保存当前上下文（寄存器、PC、栈指针等）
2. 更新 PCB/TCB 状态
3. 选择下一进程/线程（调度）
4. 恢复新上下文
5. 跳转执行

## 开销

进程切换涉及地址空间/TLB 刷新，**重于**线程切换。

## 面试一句话

> 切换即保存现场、调度、恢复现场；进程切换因页表/TLB 比线程更重。
""",
"面试题/操作系统/线程的分类.md": """# 线程的分类

| 分类 | 类型 |
|------|------|
| 按调度 | 内核级线程、用户级线程、混合模型 |
| 按作用 | 守护线程 vs 用户线程（Java `setDaemon`） |
| 按池化 | 平台线程 vs 虚拟线程（JDK 21+ Project Loom） |

## 面试一句话

> 线程分内核/用户级；Java 有守护线程，JDK21+ 还有轻量虚拟线程。
""",
"面试题/操作系统/虚拟内存.md": """# 虚拟内存

## 概念

每个进程有**独立虚拟地址空间**，通过页表映射到物理内存或磁盘交换区。

## 作用

- 逻辑地址空间大于物理内存
- 进程隔离、共享库映射
- 按需调页

## 面试一句话

> 虚拟内存用页表把虚拟地址映射到物理/磁盘，实现大地址空间与进程隔离。
""",
"面试题/操作系统/分页和页表.md": """# 分页和页表

## 分页

物理内存划分为固定大小**页框**，虚拟空间划分为**页**，大小通常相同（如 4KB）。

## 页表

| 项 | 作用 |
|----|------|
| 页号 → 页框号 | 地址转换 |
| 有效位 | 是否在内存 |
| 访问/脏位 | 置换算法用 |

多级页表、TLB 加速转换。

## 面试一句话

> 分页固定大小块映射；页表完成虚拟页到物理框的转换，TLB 加速。
""",
"面试题/操作系统/分段和段表.md": """# 分段和段表

## 分段

按逻辑单位（代码段、数据段、栈段）划分，**变长**，有段号+段内偏移。

## 段表

记录段基址、限长/权限；易产生外部碎片，常**与分页结合**（段页式）。

## 面试一句话

> 分段按逻辑模块变长划分；段表存基址与限长，常与分页组合成段页式。
""",
"面试题/操作系统/交换空间.md": """# 交换空间

磁盘上用于**换出**暂时不用的页/段的区域（swap 分区或文件）。

- 物理内存不足时，把冷页换出
- 访问时再换入，产生缺页中断

## 面试一句话

> 交换空间是磁盘上的 swap，用于内存紧张时换页，以时间换空间。
""",
"面试题/操作系统/为什么虚拟地址空间切换比较耗时.md": """# 为什么虚拟地址空间切换比较耗时

## 原因

| 因素 | 说明 |
|------|------|
| 页表切换 | 加载新进程页表基址 |
| TLB 刷新 | 地址缓存失效，需重填 |
| 缓存污染 | 新进程工作集不同 |
| 上下文保存 | 寄存器、内核栈等 |

线程切换共享地址空间，**无需**换页表，故更快。

## 面试一句话

> 进程切换要换页表并刷新 TLB、缓存，开销远大于同进程内线程切换。
""",
"面试题/操作系统/页面替换算法.md": """# 页面替换算法

| 算法 | 说明 |
|------|------|
| OPT | 最优，理论参考，不可实现 |
| FIFO | 先进先出，可能 Belady 异常 |
| LRU | 最近最久未用，接近最优，实现成本高 |
| Clock | LRU 近似，使用时钟指针 |
| LFU | 最不经常使用 |

## 面试一句话

> 缺页时用 OPT/LRU/FIFO/Clock 等选牺牲页；LRU 效果好但实现贵。
""",
"面试题/操作系统/缓冲区溢出.md": """# 缓冲区溢出

## 概念

向缓冲区写入**超过其容量**的数据，覆盖相邻栈/堆内存，可能导致崩溃或执行恶意代码。

## 防护

栈金丝雀、ASLR、DEP/NX、安全编码（边界检查）、现代语言边界检查

## 面试一句话

> 缓冲区溢出是写越界破坏内存；靠边界检查、栈保护、ASLR 等缓解。
""",
"面试题/操作系统/硬链接和软链接的区别.md": """# 硬链接和软链接的区别

| | 硬链接 | 软链接（符号链接） |
|---|--------|-------------------|
| inode | 与原文件相同 inode | 新 inode，存路径 |
| 跨分区 | 否 | 是 |
| 删原文件 | 仍可通过硬链访问 | 链接失效 |
| 目录 | 通常不允许 | 允许 |

## 面试一句话

> 硬链接同 inode 共享数据；软链接是路径别名，删源文件链接即断。
""",
"面试题/操作系统/中断和轮询的区别.md": """# 中断和轮询的区别

| | 轮询 | 中断 |
|---|------|------|
| CPU | 反复查询设备，浪费 | 设备就绪时打断 CPU |
| 延迟 | 取决于轮询频率 | 及时 |
| 适用 | 简单、实时性要求极高且已知极快完成 | 一般 IO 设备 |

## 面试一句话

> 轮询 CPU 主动问设备；中断由设备通知 CPU，省 CPU 且响应及时。
""",
"面试题/操作系统/中断的处理过程.md": """# 中断的处理过程

1. 设备发出中断请求
2. CPU 完成当前指令后响应
3. 保存现场（寄存器等）
4. 查**中断向量表**得处理程序入口
5. 执行 ISR
6. 恢复现场返回

## 面试一句话

> 中断：保存现场 → 查向量表 → 执行 ISR → 恢复现场继续执行。
""",
"面试题/项目/项目开发流程.md": """# 项目开发流程

## 常见阶段

| 阶段 | 内容 |
|------|------|
| 需求 | 调研、评审、原型 |
| 设计 | 架构、接口、库表、评审 |
| 开发 | 编码、单测、Code Review |
| 测试 | 联调、SIT/UAT、性能与安全 |
| 上线 | 灰度、发布、监控、回滚预案 |
| 运维 | 监控告警、迭代优化 |

## 敏捷补充

Sprint 计划 → 每日站会 → 评审与回顾

## 面试一句话

> 需求→设计→开发→测试→上线→运维；敏捷用迭代与站会保障交付节奏。
""",
}

def main():
    n = 0
    for rel, body in BLOCKS.items():
        p = ROOT / rel
        t = body.strip() + "\n"
        if p.read_text(encoding="utf-8", errors="replace") != t:
            p.write_text(t, encoding="utf-8")
            n += 1
            print(rel)
    print(f"BULK={n}")

if __name__ == "__main__":
    main()
