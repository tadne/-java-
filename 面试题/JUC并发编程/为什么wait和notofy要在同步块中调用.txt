wait和notify方法
	用来实现线程间的通信和协作的，需要在同步块中调用
	因为涉及到对象锁的获取和释放,wait方法会让当前线程进入等待状态，并释放对象锁，
		等待其他线程通过notify或notifyAll方法唤醒。

	notify方法会唤醒一个等待在该对象上的线程，让它重新竞争对象锁。
	notifyAll方法会唤醒所有等待在该对象上的线程，让它们重新竞争对象锁。

	如果不在同步块中调用这些方法，会抛出IllegalMonitorStateException异常，
	



	这些方法需要获取对象锁才能执行。
	如果没有对象锁，就无法实现线程间的同步和协作。