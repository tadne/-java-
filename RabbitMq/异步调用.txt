异步调用方式就是基于消息通知的方式，一般包含三个角色：

	消息发送者：	投递信息的人，即调用方
	消息代理：	管理，暂存，转发消息，可以把它理解为微信服务器
	消息接收者：	接收和处理信息的人，就是服务提供方

	消息发送者===》消息代理==》消息接收者


	优势：	解除耦合，扩展性强
			无需等待，性能好
			故障隔离
			缓存消息，流量削峰


	缺点：	不能立刻得到调用结果，时效差
			不确定业务是否执行成功
			业务安全依赖Broker的可靠性

