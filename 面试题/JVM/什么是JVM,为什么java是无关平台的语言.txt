JVM 是 Java 虚拟机的缩写，
	一种能够执行 Java 字节码的虚拟机进程。
	
	Java 源文件（.java）被编译器编译成字节码文件（.class），
	然后字节码文件被 JVM 解释成不同平台所需要的机器码，从而实现了 Java 程序的跨平台运行。

Java 之所以被称为平台无关的语言，
	是因为 Java 程序在编译成字节码后，可以在任何支持 JVM 的操作系统上运行，
	而不需要重新编译。这使得 Java 程序具有很强的可移植性和跨平台性。


