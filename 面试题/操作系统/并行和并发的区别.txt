并行和并发的区别

	并发:	在一段时间内,多个任务会被处理,但是某一时刻只有一个任务执行
			单核处理器可以做到并发,如:	两个线程a和b,a运行时间片后,切换到b,b运行时间片后切换到a
				因为切换速度足够快,宏观上表现为一段时间能同时运行多个程序	

			所以其实并发不一定多个线程,也可能是一个线程交替执行多个任务

	并行:	在同一时刻,有多个任务执行.
			需要多核处理器才能完成,在微观上就能同时执行多条指令,不同的程序被放到不同的处理器上运行
			是物理上的多个线程同时运行

			并行就是多个线程同时执行任务