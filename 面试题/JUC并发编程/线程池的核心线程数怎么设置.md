# 线程池的核心线程数怎么设置

无万能公式，需结合 **CPU/IO 比例** 与压测。

## 经验起点

| 类型 | 参考 |
|------|------|
| CPU 密集 | `N` 或 `N+1`（N=核数） |
| IO 密集 | `2N` 或 `N/(1-阻塞系数)` |
| 混合型 | 拆分线程池 |

## 注意

- 队列有界 + 拒绝策略，避免 `Executors` 无界队列 OOM。
- 业务线程池与定时/IO 池隔离。

## 面试一句话

> CPU 密集接近核数，IO 密集可更大；最终以压测为准，并配有限队列与拒绝策略。
