Bean依赖注入注解开发
	
	@Value		用在字段方法上，用于注入数据
	@Autowired	用在字段方法上，用于根据类型注入bean
		根据类型进行匹配，如果类型有多个，就根据名字二次匹配		
		当用在方法上的时候，Autowired匹配的是方法上的参数类型
			如果这个类型为某个集合或者什么的泛型，
			那么Autowired会将所有符合类型的bean放入参数中
	
	@Qualifier	用在字段方法上，结合@Autowired，根据名称注入
	@Resource	用在字段方法上，根据类型或名称注入（这个取决于是否引入name参数）
			
	
	
	
