通用命令:

	help [command]:		可以查看一个命令的具体用法
	keys :				查看符合模版的所有key,但是不建议在实际开发中使用
	del:					删除指定的key,可以同时删除多个,返回值为删除的数量
	exist:				判断key是否存在
	expire:				给key设置有效期,有效期到了自动删除,单位是秒
	ttl:					查看key的剩余有效期,不限制有效期就返回-1
	




