有没有可能两个不相等对象有相同hashcode

	有可能,这种情况叫做hash冲突
	
	采取一些措施来区分它们。一般有以下几种方法：

	拉链法：	每个哈希表节点都有一个next指针，多个哈希表节点可以用next指针构成一个单向链表，
		被分配到同一个索引上的多个节点可以用这个单向链表进行存储。

	开放定址法：
		一旦发生了冲突，就去寻找下一个空的散列地址，只要散列表足够大，
		空的散列地址总能找到，并将记录存入。

	再哈希法：又叫双哈希法，有多个不同的Hash函数。
		当发生冲突时，使用第二个、第三个……等哈希函数计算地址，直到无冲突。